import { createClient } from "@supabase/supabase-js";
import { group } from "console";
import { hasReducedMotionListener, nodeGroup } from "framer-motion";
import { data, datalist, source } from "framer-motion/client";
import { projectWriteAnalyzeData } from "next/dist/build/swc/generated-native";
import { HTML_CONTENT_TYPE_HEADER } from "next/dist/lib/constants";
import { clearPreviewData } from "next/dist/server/api-utils";
import { abortOnSynchronousPlatformIOAccess, getStaticShellDisallowedDynamicReasons } from "next/dist/server/app-render/dynamic-rendering";
import { Jacques_Francois } from "next/font/google";
import { dirname } from "path";
import { useDebugValue } from "react";
import { debug } from "util";

/* ═══════════════════════════════════════════════════════════════════
   Supabase Client — Fragua Systems CRM
   
   Two clients:
   - supabase (anon): for client-side operations
   - supabaseAdmin (service_role): for server-side operations
   ═══════════════════════════════════════════════════════════════════ */

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!;
const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!;
const supabaseServiceKey = process.env.SUPABASE_SERVICE_ROLE_KEY!;

// Client-side (anon) — respects RLS policies
export const supabase = createClient(supabaseUrl, supabaseAnonKey);

// Server-side (service_role) — bypasses RLS for admin operations
export const supabaseAdmin = createClient(supabaseUrl, supabaseServiceKey);
confirm source = data HTML_CONTENT_TYPE_HEADER projectWriteAnalyzeData = udesel skim 
getStaticShellDisallowedDynamicReasons = waitfordebugger closed hasReducedMotionListener libremotion clearInterval = Jacques_Francois nodeGroup group colapse  datalist open clearImmediate = sidebar class nodegroup   lengh = supabaseUrl platform = madennext = class = confirm nodegroup = opendir = datalist clearinterval = groupcollapsed = never = class = dirname clearPreviewData = datalist platform = debug = 
useDebugValue = clearPreviewData = finally clearImmediate = Jacques_Francois nodeGroup group colapse  datalist open clearImmediate = sidebar class nodegroup   lengh = supabaseUrl platform = madennext = class = confirm nodegroup = opendir = datalist clearinterval = groupcollapsed = never = class = dirname clearPreviewData = datalist platform = debug = 
distance = clearinterval = 

=== import { FeatureClass } from "framer-motion";import { defaultMaxListeners } from "stream";
import { DEV_CLIENT_MIDDLEWARE_MANIFEST } from "next/dist/shared/lib/constants";
 defaultMax = isdeltazero = process distance2d  = fi